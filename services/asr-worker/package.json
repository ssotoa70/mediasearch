{
  "name": "@mediasearch/asr-worker",
  "version": "1.0.0",
  "description": "ASR Worker service - CPU/GPU transcription workers",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/"
  },
  "dependencies": {
    "@mediasearch/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
